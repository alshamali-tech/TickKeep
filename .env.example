# TimeVault — build-time configuration (all optional)
# Copy to .env and fill in for your deployment.

# Public URL of the deployed site (used only in docs/sitemap examples today)
VITE_APP_URL=https://tickkeep.app

# Donation link — shown in the footer, sidebar and Settings → About.
# A plain external link; no provider JS is ever loaded.
VITE_KOFI_URL=https://ko-fi.com/mammonalshamali
